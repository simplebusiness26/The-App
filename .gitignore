node_modules/
.expo/
dist/
# The CI web export, and any local copy of it. `dist/` alone did not cover
# dist-ci, so a build got committed by a git add -A and had to be taken back out.
dist-ci/
dist-*/
.env
.env.*
!.env.example
*.log
.DS_Store

# Generated by `expo prebuild` for local APK builds. The app is managed --
# app.config.js is the source of truth for native configuration.
/android/
/ios/
